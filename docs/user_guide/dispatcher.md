# Dispatcher

The RAM-aware dispatcher models circular DMA buffers and flash-backed weight streaming for tight stack budgets (see `DispatcherConfig` defaults).

See [Dispatcher API](../api/dispatcher.md) and `dispatcher_cli.py` for command-line entry points.
