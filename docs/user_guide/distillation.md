# Distillation

Knowledge distillation transfers a teacher policy (PPO/A2C) to a smaller student network suitable for edge devices.

Use `distill.py` or the `tinyrl.distillation` API after `pip install -e .`. See [Distillation API](../api/distillation.md).
