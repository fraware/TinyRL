# Verification

SMT-style checks and Lean-oriented proof scaffolding help validate quantized policies. A full Lean/Lake pipeline is **not** bundled; treat Lean snippets as optional unless you add that toolchain.

Optional extra:

```bash
pip install -e ".[verify]"
```

See [Verification API](../api/verification.md) and `verify_cli.py`.
