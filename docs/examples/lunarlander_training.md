# LunarLander training

```bash
pip install -e .
python train.py --config configs/train/a2c_lunarlander.yaml --no-wandb
```

Tune hyperparameters in `configs/train/a2c_lunarlander.yaml` or copy the file for a new experiment.
